<%= link_to "Add a post", new_post_path, class:"btn btn-success pull-right" %>
<h2> All Posts </h2>
<% @posts.each do |post| %>
  <h2> <%= link_to post.title, post_path(post.id) %> </h2>
  <p><%= post.content %></p>
<% end %>

<%= paginate @posts %>